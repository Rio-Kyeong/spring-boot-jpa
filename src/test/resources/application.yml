server:
  port: 80

#spring:
#  datasource:
#   url: jdbc:h2:mem:testdb
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
      # 실행 시점에 drop 후 create 된다.
      # 종료 시점에 drop 으로 초기화를 시킨다.
#      ddl-auto: create-drop
#    properties:
#      hibernate:
        # show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace